[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "neural-bamlss"
version = "0.1.0.dev0"
description = "Bayesian additive models for location, scale, and shape — neural networks with mean-field VI"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.12",
    "numpy",
    "pandas>=2.2",
    "arviz",
    "scikit-learn",
    "matplotlib",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "mypy",
    "pandas-stubs",
]

[tool.hatch.build.targets.wheel]
packages = ["src/neural_bamlss"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
