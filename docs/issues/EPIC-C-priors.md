# Epic: Goal 2 — Priors as regularization & random effects

> GitHub: #19 · Source: PRD `docs/prd/0001-bayesian-feature-networks.md` (#1)

The per-feature prior scale as an mgcv smoothing parameter across three tiers
(fixed / empirical-Bayes / hierarchical), and its reuse as the variance component that
turns categorical effects into BAMLSS-style random effects with partial pooling.

## Encompasses

- 0011 / #12 — PriorScale handle: empirical-Bayes + hierarchical tiers
- 0012 / #13 — Categoricals as random effects
