# Smoke result

CI-scale acceptance artifact for GitHub #103: four held-out Auto MPG rows,
one training epoch, 16 posterior draws, and 32 predictive samples. It proves
the shared adapter and combined table end-to-end; it is not paper evidence.
The complete panel must be run with the checked-in top-level `config.yaml`
and deliberately promoted separately.
